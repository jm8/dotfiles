#!/usr/bin/env zsh
dir=$(ls -lah /proc/$(pgrep -P $(xdotool getwindowpid $(xdotool getwindowfocus)) zsh)/cwd | awk '{print $NF}')
if [ -d "$dir" ]; then
  wezterm start --always-new-process --cwd "$dir"
else
  wezterm start --always-new-process
fi
